name: CI and Deploy for Person API

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: 3.12

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      run: python manage.py test

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Log in to Railway
      env:
        RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}
      run: |
        echo $RAILWAY_TOKEN | docker login -u maksimka.mishura@mail.ru --password-stdin registry.railway.app

    - name: Build and Push Docker Image
      run: |
        docker build -t registry.railway.app/728a46ed-7964-43ba-99de-1c7789f004dc/RSOI_lab1:latest .
        docker push registry.railway.app/728a46ed-7964-43ba-99de-1c7789f004dc/RSOI_lab1:latest
